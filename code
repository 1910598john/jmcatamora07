// const currentDate = new Date();
// // Get the current month
// const currentMonth = currentDate.getMonth();
// // Create a new Date object for the next month
// const nextMonth = new Date(currentDate.getFullYear(), currentMonth + 1, 1);
// // Subtract 1 day from the next month to get the last day of the current month
// const lastDayOfMonth = new Date(nextMonth - 1);
// // Get the day of the month from the last day
// const numberOfDays = lastDayOfMonth.getDate();
// document.write("Number of days in the current month: ", numberOfDays);